import Axios from 'axios';

// twitter bots
// intext
// portfolio

// name
// description
// url
// languages 'https://api.github.com/repos/logankimbs/{repo}/languages'
// picture?

export default async function fetchProjects() {
    try {
    } catch (error) {
        console.error(error);
    }
}
